# W1-02 Strategy A Daily — v5 Re-review (backtest-reviewer agent)

**Date**: 2026-04-15
**Reviewer**: backtest-reviewer (general-purpose subagent, adversarial external-auditor posture)
**Scope**: Re-review after v4→v5 schema-mirror change (W1-03 v3 symmetry)
**Sections applied**: A, B, C, F, G, H (D, E N/A — frozen parquet, no Wilder indicators)

## Change under review

Single delta vs v4:

- `research/_tools/make_notebook_02.py` (cell 11 / line ~377):
  - Before: `'deepest_dd_reconciles_with_max_drawdown': bool(abs(deepest_dd_pct - max_dd) < 1e-9),`
  - After:  `'deepest_dd_reconciles_with_max_drawdown': (bool(abs(deepest_dd_pct - max_dd) < 1e-9) if total_trades > 0 else None),`

- Notebook regenerated and executed via `jupyter nbconvert --execute --inplace` (0 errors).
- Numerics claimed unchanged vs v4.

## Verification performed

1. Read generator `research/_tools/make_notebook_02.py` end-to-end. Confirmed guard is the sole substantive change in cell 11; all other cells byte-identical to v4 intent.
2. Grep confirmed executed notebook `research/notebooks/02_strategy_a_trend_daily.ipynb` line 651 contains the guarded expression verbatim — generator and notebook are in sync.
3. Read `research/outputs/strategy_a_daily.json`:
   - `metrics.sharpe = 1.0352900037639534` — matches claim exactly.
   - `metrics.total_trades = 14` — matches.
   - `metrics.deepest_drawdown.pct = -0.2245037367777342`, `duration_days = 480`, `status = "Active"` — matches.
   - `metrics.longest_drawdown.pct = -0.15291167058118668`, `duration_days = 537`, `status = "Recovered"` — matches.
   - `edge_case_checks.deepest_dd_reconciles_with_max_drawdown = true` — correct: with `total_trades = 14 > 0`, the guarded ternary evaluates to `bool(abs(dd - max_dd) < 1e-9) = True`. Semantics preserved for the live (N>0) path.
4. Read `.evidence/w1-02-strategy-a-daily.txt` — v5 header present, change documented, no stale claims.
5. Read `docs/stage1-subplans/w1-02-strategy-a-daily.md` — status Done, acceptance criteria intact, parameters match generator constants.

## Checklist findings

### A. Data Integrity — PASS
- Cell 3: SHA256 verification with `assert DATA_HASH == expected_hash`.
- Cell 4: `assert df.index.tz is not None and str(df.index.tz) == 'UTC'`. pyupbit naive-KST → UTC was enforced upstream (W1-01).
- Data file: `KRW-BTC_1d_frozen_20260412.parquet`, hash `da5b5a5b...`, 1927 bars, NaN=0.
- No gap-check in this notebook, but W1-01 provenance covers it (consumer rule satisfied).

### B. Pre-registered Parameters — PASS
- Six constants declared in cell 5 (MA_PERIOD=200, DONCHIAN_HIGH=20, DONCHIAN_LOW=10, VOL_AVG_PERIOD=20, VOL_MULT=1.5, SL_PCT=0.08).
- JSON `parameters` block mirrors all six exactly.
- No sensitivity grid present. Go/No-Go evaluated solely on pre-registered values.
- DSR N/A (Week 1).

### C. vectorbt 0.28.5 API — PASS
- `sl_stop=0.08` (fraction), `sl_trail=False` (bool), `freq='1D'`.
- `ts_stop` / `td_stop` / `max_duration` / `time_stop` / `dt_stop`: absent (grep-clean).
- All metric accessors called with parens: `pf.sharpe_ratio()`, `pf.total_return()`, `pf.max_drawdown()`, `pf.trades.count()`, `pf.trades.win_rate()`, `pf.trades.profit_factor()`, `pf.drawdowns.records_readable`, `pf.value()`.
- No undefined-variable references in exit mask (`entry_price`, `bars_held`, etc.).

### F. Strategy Logic / No-lookahead — PASS
- `ma200 = close.rolling(200).mean()` — MA_PERIOD name truly matches window.
- `donchian_high = high.rolling(20).max().shift(1)` and `donchian_low = low.rolling(10).min().shift(1)` — shift(1) present.
- `vol_avg = volume.rolling(20).mean().shift(1)` — volume shift(1) present (critical look-ahead guard).
- Entry mask `(close > ma200) & (close > donchian_high) & (volume > vol_avg * 1.5)` — since the channel/vol variables were pre-shifted, no double-shift; EOD semantics are documented in markdown cell 1 and code cell 7.
- Warmup zero-entries hard asserted via `entries.iloc[:MA_PERIOD].sum() == 0`.
- Volume filter efficacy hard asserted via `filtered_out > 0` (actual: 45 rejected).
- No dead indicators (ATR deliberately deferred to W1-04; comment present).
- No time-stop in this task, so the `entries.shift(N)` caveat-comment rule does not apply.

### G. Output / Evidence schema — PASS (with the v5 guard intended)
- JSON contains all required fields: `feature_id`, `task_id`, `data_hash`, `parameters`, `sharpe`, `total_return`, `max_drawdown`, `win_rate`, `profit_factor`, `total_trades`, plus the enriched DD, SE/CI, and edge-case blocks.
- `data_hash` is a bound variable (`DATA_HASH`) computed via `sha256_file()` and written into JSON — no string-literal cheat.
- `freq='1D'` matches the daily parquet.
- Equity PNG generated.
- **v5 change verified live**: with N=14 trades, the ternary evaluates the true branch, yielding `true` in JSON. Structural symmetry with W1-03 v3 achieved without perturbing numerics or observable behavior on the populated path. On the 0-trade path the field now emits `null` instead of a meaningless `true` (abs(nan - nan) is False, but 0-trade execution would hit the prior `sharpe NaN` path anyway; emitting `None` is the more honest serialization and avoids a silent False that could be misread as "reconciliation failed").
- Evidence file follows 6-part structure and references prior agent traces.

### H. Cross-document Consistency — PASS
- sub-plan status: Done; parameters, Feature ID (STR-A-001), task_id (W1-02) all match JSON.
- Acceptance criteria all PASS in evidence.
- research/CLAUDE.md module context already reflects "W1-02 v5 완료".
- execution-plan dependency matrix (W1-02 → W1-04/W1-05/W1-06) unchanged and still valid.

## Adversarial probes (attempted rubber-stamp failures)

- Did the guard change any observable value on the populated path? No — `bool(abs(dd - max_dd) < 1e-9)` is still executed verbatim when `total_trades > 0`. Confirmed in JSON: `true`, identical to v4.
- Could the `None` branch ever fire accidentally with trades present? No — `total_trades` is `int(pf.trades.count())`, only 0 when no trades; and the gate uses strict `> 0`.
- Is the generator-vs-notebook drift risk real? Checked: line 651 of the executed notebook matches line 377 of the generator exactly.
- Are numerics drift-free vs claim (Sharpe 1.0352900037639534, trades 14, deepest -22.45% 480d Active, longest -15.29% 537d Recovered)? Confirmed bit-for-bit against `strategy_a_daily.json`.
- Does the evidence still cite older agent traces as APPROVED authority without a v5 trace? It cites v2/v3/v4 traces and flags this v5 change; once this file is saved the chain is complete.
- Any emoji / lookahead / undefined-var regression slipped in? Grep-clean across the generator.

## Findings

- BLOCKING: 0
- WARNING: 0
- NIT: 0 (the change is a pure robustness/symmetry improvement; no new suggestions)

## Verdict

APPROVED
