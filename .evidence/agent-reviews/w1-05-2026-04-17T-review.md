# Backtest Review: W1-05 4h Porting Experiment (BT-002)

**Date**: 2026-04-17
**Reviewer**: backtest-reviewer agent
**Artifact**: `research/_tools/make_notebook_05.py`, `research/outputs/strategy_4h_comparison.json`, `.evidence/w1-05-4h-experiment.txt`

---

## BLOCKING

### [BLOCKING-1] Evidence win_rate mismatch vs JSON

**File**: `.evidence/w1-05-4h-experiment.txt` lines 33, 35

Evidence table shows A 4h win_rate = 55.00% and B 4h win_rate = 45.41%.
JSON shows A 4h win_rate = 0.5 (50.00%) and B 4h win_rate = 0.6039 (60.39%).
Both values are wrong in the evidence file. This undermines evidence trustworthiness.

**Fix**: Regenerate evidence table from JSON or re-run notebook and update both.

---

## WARNING

### [WARNING-1] Sub-plan lists ATR_PERIOD = 14 for Strategy A 4h (line 67)

Sub-plan `w1-05-4h-experiment.md` SubTask W1-05.2 declares `ATR_PERIOD = 14` as a pre-registered parameter for Strategy A. Strategy A does NOT use ATR (confirmed: zero ATR references in `make_notebook_05.py`). The notebook correctly omits ATR, but the sub-plan's parameter list is misleading and creates a spec-vs-implementation discrepancy.

**Fix**: Remove `ATR_PERIOD = 14` line from sub-plan W1-05.2 parameter block, or add a note that it is unused dead spec.

### [WARNING-2] Time stop caveat comment missing

Checklist F requires: when using `entries.shift(N)`, a comment stating "entry N bars after approximation, not actual held N bars" must be present. `run_strategy_b()` uses `entries.shift(time_stop_bars)` (line 176) with no such caveat comment.

**Fix**: Add caveat comment per checklist item F.

---

## NIT

### [NIT-1] No `sha256_hash` variable explicitly bound for 4h in JSON

The JSON stores hashes under `data.4h.hash` and `data.1d.hash` (correct), but checklist G requires that `sha256_hash` or `data_hash` variable is explicitly assigned in the notebook. The notebook uses `DATA_HASH_4H` and `DATA_HASH_1D` which satisfies the intent. No action needed, noting for completeness.

---

## Checklist Summary

| Section | Verdict | Notes |
|---------|---------|-------|
| A. Data Integrity | PASS | Hash assert both 4h and 1d, UTC check, monotonic |
| B. Pre-registered Params | PASS | Constants declared with conversion comments |
| C. vectorbt 0.28.5 API | PASS | sl_stop fraction, sl_trail bool, freq='4h' lowercase, no banned APIs |
| D. pyupbit | N/A | Not used in this notebook |
| E. Wilder Smoothing | PASS | RSIIndicator from ta library |
| F. Strategy Logic | PASS (with WARNING-2) | shift(1) on Donchian/volume, warmup assert, window x6 correct |
| G. Output/Evidence | **FAIL** (BLOCKING-1) | Evidence win_rate does not match JSON |
| H. Cross-doc | PASS (with WARNING-1) | BT-002 consistent, sub-plan ATR_PERIOD is dead spec |

**Window conversions**: MA200->1200, DH20->120, DL10->60, VolAvg20->120, TimeStop5->30, RSI stays 4. All correct.
**freq='4h'**: Correct lowercase. Used in both `run_strategy_a` and `run_strategy_b` calls.
**Strategy function parity**: `run_strategy_a/b()` logic matches W1-02/W1-03 patterns with freq parameterized. Confirmed.
**Reference-only rule**: Explicitly stated in markdown header, JSON `note` field, comparison table header, and summary cell.
**1d validation**: Cell 7 reproduces 1d Sharpe with tolerance 1e-4 assertion before running 4h. Confirmed.

---

**Findings**: 1 BLOCKING, 2 WARNING, 1 NIT

## CHANGES REQUIRED

Fix BLOCKING-1 (evidence win_rate mismatch) before APPROVED status can be granted.
