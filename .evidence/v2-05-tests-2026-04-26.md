# V2-05 Tests + Real Upbit Auth Sanity — Evidence

Task: V2-05 (pytest 설정 + unit + integration + 실 Upbit 인증)
Feature: STAGE1-V2-010
Date: 2026-04-26
Status: Done — **57 tests PASS** + **실 Upbit 인증 sanity PASS**
Sub-plan: `docs/stage1-v2-relaunch.md` v2 §2.1 (V2-05)

---

## 1. 신설 (~700 LOC)

```
engine/
├── pytest.ini              # markers + addopts
└── tests/
    ├── __init__.py
    ├── conftest.py         # tmp_state / synth_ohlcv / breakout_ohlcv / sl_ohlcv fixtures
    ├── test_state.py       # 12 tests
    ├── test_strategies.py  # 11 tests
    ├── test_order.py       # 16 tests
    ├── test_position.py    # 10 tests
    └── test_integration.py # 8 tests
```

## 2. 테스트 결과

```
tests/test_state.py        ........ 12 passed
tests/test_strategies.py   ........ 11 passed
tests/test_order.py        ........ 16 passed
tests/test_position.py     ........ 10 passed
tests/test_integration.py  ........  8 passed
=================================== 57 passed in 0.36s
```

### 커버리지 핵심

- **state**: meta / position upsert/list / order upsert + status transition / idempotency / restart 시나리오
- **strategies**: warmup 미충족 ValueError / hold / SL 터치 / indicator snapshot / 누락 컬럼 ValueError
- **order**:
  - make_client_oid deterministic (W-1 정정 검증)
  - _safe_float (ccxt #7235 defensive)
  - paper buy/sell + 멱등성 (동일 oid 재호출 → 동일 record)
  - **live mock: status='wait' → _immediate_poll → 'done' 전이** (W-2 정정 검증)
  - live error response → RuntimeError
  - paper cancel False / live cancel True
- **position**:
  - open from filled buy / unrealized PnL profit & loss
  - close → realized PnL 계산 (entry fees + exit fees 포함)
  - **trades-YYYY.jsonl 세금 데이터 entry+exit 2행 기록** (CLAUDE.md 박제 검증)
  - close 후 position None / 잘못된 입력 거부
- **integration**:
  - Engine init 3 cells / notifier disabled
  - has_pending_order False/True 전이 (C-2 정정 검증)
  - sync_open_orders polled=0 / promoted_buy=1 / promoted_sell=1 (C-1 정정 검증)
  - run_cycle hold scenario (last_run_ts 박제 + 0 주문)

## 3. 실 Upbit 인증 Sanity (사용자 API 키 발급 후 1회)

**전제**: 사용자 1~5단계 완료 (계정 + KYC + K뱅크 + API 키 발급 + Keychain 저장 + 고정 IP) — 2026-04-26 사용자 보고 "5단계까진 했어".

**호출 (read-only만, 안전)**:
```python
upbit = pyupbit.Upbit(access, secret)  # Keychain에서 로드
upbit.get_balance("KRW")
upbit.get_balances()
```

**결과**:
```
Keychain access key 발견 (length=40, prefix=<masked>)
Keychain secret key 발견 (length=40)

pyupbit.Upbit 인스턴스 생성 OK

[get_balance KRW] = 0
  KRW 잔고 조회 OK (실제 입금 여부: 0원 (페이퍼 단계 OK))

[get_balances] 리스트 길이=0
```

**확인 사항**:
- Access/Secret Key 형식: **40자** (이전 가이드 "공식 미공개" → 실측 확인)
- Keychain 저장 정상 작동 (`security` 명령 + `get_keychain_secret`)
- pyupbit.Upbit 인증 호출 정상 (HTTP 200, 권한 정상)
- 잔고 0원 (페이퍼 단계, K뱅크 입금 X) — 정상
- 보유 코인 0 — 정상 (Stage 1 v1 학습 모드 후 첫 라이브 봇)

## 4. 미실측 (의도적, V2-06/07 책무)

- 실제 매수/매도 호출 — 페이퍼는 mock으로 충분, 라이브는 V2-07 (10만원 입금 후)
- 24시간 daemon 모드 안정성 — V2-06 (페이퍼 2~4주 운영)
- Discord webhook 실 전송 — 사용자 6단계 완료 시점 작동 (현재 webhook 발급됨, 테스트는 격리)
- 백테스트 ↔ 페이퍼 ±30% 일치 — V2-06 데이터 누적 후

## 5. 룰 준수

- pytest 설정 표준 (markers / addopts / filterwarnings)
- mock 기반 격리 (실 네트워크 호출 X, 단 sanity 1회 read-only는 별도)
- tmp_path fixture로 SQLite/logs 격리
- monkeypatch로 ENGINE_ROOT 격리 (테스트 환경 ↔ 실 환경 분리)
- ccxt #7235 defensive (`_safe_float` 단위 테스트 포함)
- 멱등성 deterministic (W-1 정정 검증)
- 라이브 비동기 fill (W-2 정정 검증)
- C-1/C-2 정정 (sync_open_orders / has_pending_order) 검증

## 6. 다음 단계

### V2-06 진입 가능 조건
- ✓ V2-05 tests + 실 Upbit 인증 PASS
- ✓ 사용자 1~5단계 완료
- ✓ Discord webhook 발급 (6단계, 사용자 자체 완료)
- ⏳ 페이퍼 트레이딩 daemon 시작 (`python -m engine.main`)
- ⏳ 2~4주 관측 + 백테스트 ↔ 페이퍼 오차 기록

### 추가 권고
- launchd plist (macOS 24/7 daemon) — V2-06 시작 시
- 백테스트 ↔ 페이퍼 비교 도구 (small script) — V2-06 1주차 마감 시점

---

End of V2-05 evidence. Generated 2026-04-26.
