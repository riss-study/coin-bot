# V2-02 Module 골격 — Evidence

Task: V2-02 (logger + state + market_data)
Feature: STAGE1-V2-004
Date: 2026-04-24
Status: Done (3 모듈 sanity 전부 PASS)
Sub-plan: `docs/stage1-v2-relaunch.md` v2 §2.1 / §2.4

---

## 1. 데이터 입력

- Upbit public API (인증 불필요): OHLCV, 현재가, 호가
- SQLite DB (data/state.sqlite, WAL 모드)
- JSONL 로그 파일 (logs/engine-YYYYMMDD.log + trades-YYYY.jsonl + signals-YYYYMMDD.jsonl)

## 2. 사전 지정 설계

### logger.py
- JSONL 포맷 + console handler
- 일반 로그 / 거래 (trades-YYYY.jsonl) / 신호 (signals-YYYYMMDD.jsonl) 3종 분리
- 거래 기록 세금 신고 표준 필드 (ts_utc/kst, pair, strategy, side, order_type, price_krw, volume, krw_amount, fees_krw, slippage_bps, order_uuid, client_oid, run_mode)

### state.py
- SQLite WAL 모드 + thread-safe (RLock)
- 4 테이블: meta / positions / orders / idempotency
- client_oid → order_uuid 매핑 (재시도 시 이중 주문 방지)
- 재시작 복원: list_open_positions() + list_open_orders()

### market_data.py
- pyupbit 0.2.34 wrapper
- 지수 백오프 재시도 (DEFAULT_RETRY_MAX=5)
- UTC 타임존 강제 (research/CLAUDE.md Don'ts 박제)
- get_ohlcv / get_ohlcv_from / get_current_price / get_orderbook

## 3. 결과

### 3.1 logger.py sanity

```
logger_initialized (log_file: logs/engine-20260424.log)
sanity_check_start / test_warning / sanity_check_done
files_in_logs: ['engine-20260424.log', 'signals-20260424.jsonl', 'trades-2026.jsonl']
```

### 3.2 state.py sanity

```
last_run_ts: 2026-04-24T08:02:04+00:00
position upsert/get OK
order record + idempotency lookup OK
open positions/orders 복원 OK
DB: engine/data/state.sqlite
```

### 3.3 market_data.py sanity (실 Upbit API 호출)

```
KRW-BTC 최근 5일 OHLCV (2026-04-20 ~ 2026-04-24)
  2026-04-20: open 110,040,000 / close 112,455,000 / volume 1,322
  2026-04-21: open 112,455,000 / close 113,049,000
  2026-04-22: open 113,049,000 / close 115,901,000
  2026-04-23: open 115,909,000 / close 116,429,000
  2026-04-24: open 116,430,000 / close 115,769,000 / volume 526
  index tz: UTC ✓ (KST → UTC 자동 변환)

KRW-ETH 현재가: 3,444,000 KRW
KRW-BTC 호가 bid 115,743,000 / ask 115,769,000 / spread 26,000
```

## 4. 자동 검증

- logger JSONL 포맷 파싱 가능 ✓
- state.py SQLite WAL 모드 활성 ✓
- UTC tz-aware DataFrame ✓
- Upbit public API 실 호출 성공 (인증 불필요) ✓
- 재시도 wrapper (지수 백오프) 정상 ✓

## 5. 룰 준수

- pyupbit 0.2.34 `get_ohlcv` / `get_ohlcv_from` / `get_current_price` / `get_orderbook` 시그니처 실측 일치 ✓
- UTC 타임존 강제 (research/CLAUDE.md Don'ts: "타임존 누락 금지") ✓
- pyupbit None 반환 처리 (재시도 wrapper) ✓
- WAL 모드 + thread-safe 락 ✓
- 세금 데이터 영구 저장 (CLAUDE.md 박제) ✓
- 멱등성 (client_oid ↔ order_uuid 매핑) 설계 ✓

## 6. 리뷰

- backtest-reviewer: N/A (시세/상태/로깅 인프라, 백테스트 아님)
- 외부 감사: V2-04 완료 후 호출 예정
- 사용자 승인: "그러고 나서 클로드 작업 시작하자" (2026-04-24)

## 7. 검증 라운드 + 정정 내역 (2026-04-24 2차)

외부 감사관 페르소나 재검증 결과 CRITICAL 2 + WARNING 6 + NIT 3 발견 → 정정 후 2차 재검증 PASS.

### 정정 내역

| ID | 발견 | 정정 |
|----|------|------|
| C-1 | market_data `fetch_ohlcv_range` 타임존 로컬 의존 (`astimezone()` no-arg) | `ZoneInfo('Asia/Seoul')` 명시 + naive datetime ValueError |
| C-2 | `fetch_ohlcv_range` 실 Upbit 호출 sanity 없음 | sanity 추가 (30 bars, UTC index tz 검증) |
| W-1 | state.py read-only도 `BEGIN IMMEDIATE` (writer lock) | `_conn(write=False)` → `BEGIN DEFERRED` 분리, 6곳 적용 |
| W-2 | logger.py `setup_logger` 재호출 시 logs_dir 무시 | `force: bool = False` 파라미터 추가 |
| W-3 | user-setup-guide Discord rate limit 수치 실측 X (cycle 1 #16 재발 잠재) | "공식 docs 동적 헤더 기반" 정정 + 429/Retry-After 처리 언급 |
| W-4 | market_data `get_current_price` 반환 타입 float/dict 모호 | `get_current_price(str) → float` + `get_current_prices(list) → dict` 분리 + TypeError 거부 |
| W-5 | state.py `close_position` 연관 exit 주문 업데이트 수동 | docstring 명시 (record_order → close_position 순서) |
| W-6 | config.yaml stake_amount ×3 < init_cash | 주석 추가 (라이브 전환 시 재조정) |

### 재sanity 결과 (2026-04-24 17:18)

```
config 로드 OK
logger 3종 (engine/trades/signals) 생성 OK
state: SQLite WAL + DEFERRED/IMMEDIATE 분리, 재시작 복원 OK
market_data:
  KRW-BTC 최근 5일 OHLCV (UTC tz) ✓
  KRW-BTC 지난 30일 range (30 bars, UTC) ✓  ← C-2 신규
  KRW-ETH 현재가 3,452,000 KRW (float type) ✓
  다중 현재가 dict 반환 OK ✓  ← W-4 신규
  KRW-BTC 호가 bid/ask 차 1,000 KRW
  get_current_price([...]) 거부 ✓ (TypeError)
  fetch_ohlcv_range(naive) 거부 ✓ (ValueError)
```

## 8. 다음 단계 (V2-03)

- `engine/strategies/strategy_a.py`: Trend Following (MA200 + Donchian + Volume, W2-02 v5 파라미터)
- `engine/strategies/strategy_d.py`: Volatility Breakout (Keltner + Bollinger, W2-02 v5 + W2-03.1 방법 B)
- `engine/order.py`: 주문 생성/취소/조회 + 재시도 + 멱등성 통합 (state.py register_idempotency 활용)
- 예상 3~5일

---

End of V2-02 evidence. Generated 2026-04-24 (정정 2차 포함).
