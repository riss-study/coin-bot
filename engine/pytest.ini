[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    integration: requires real Upbit/Discord network
    slow: takes >5s
filterwarnings =
    ignore::DeprecationWarning:pyupbit.*
